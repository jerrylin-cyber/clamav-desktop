# Todo - 2026-06-13

依據 `tasks.md` 目前進度整理，供下一輪工作排序參考。

## 目前狀態總覽

| Phase | 狀態 | 備註 |
|------|------|------|
| 0 專案初始化 | 完成 | |
| 1 Runtime Bundle | 移出主線 | 新規格改為 Homebrew ClamAV 偵測與引導，不再需要 app-managed runtime artifact |
| 2 Shared Runtime Installer | 移出主線 | 新規格不再做 privileged installer apply；既有 installer 程式保留作歷史/診斷 |
| 3 Runtime Resolver | 完成 | |
| 4 Database Update | 完成 | |
| 5 ClamD Client | 完成 | |
| 6 Scan Job Manager | 完成 | |
| 7 GUI | 完成 | 已改為 Homebrew ClamAV blocking setup popup，未通過檢測前不能關閉繼續使用 |
| 8 File Actions | 完成 | T8.1/8.2/8.3 |
| 9 Settings | 完成 | 已補 runtime 重新檢測、移除提示、Full Disk Access、Notifications、login/background toggles |
| 10 Scheduler / Background | 完成 | T10.1（SchedulerService）、T10.2（UpdateSchedulerService）、T10.3（PowerPolicyService）與 App 背景 worker 已完成 |
| 11 Status Bar / Login | 完成 | 已加入 NSStatusItem；login item 優先 SMAppService，失敗時 fallback per-user LaunchAgent |
| 12 Uninstaller | 完成 | 新規格改為提示移除 Homebrew ClamAV；T12.2 使用者資料移除選項已完成 |
| 13 Packaging/Signing/Notarization | 未開始 | 需 Apple 開發者憑證與簽署環境，**延後** |

## 建議下一步順序

### 1. T10.2 建立 `UpdateSchedulerService`（已完成）

- 與 T10.1 同模式：daily 觸發時間計算（可重用 `scheduler.go` 的 `nextScheduledTime`）。
- 加入 jitter（依 hostname/使用者做 deterministic offset），避免多台/多使用者同時更新。
- `RuntimeMode == "system-shared"` 時回傳「由 system updater 管理，不執行」的 defer 決策，呼叫端不應再呼叫 `FreshclamService.UpdateDatabase`。
- 串接 T10.3 `PowerPolicyService.ShouldDefer`。
- 已完成並補測試。

### 2. 將 SchedulerService / UpdateSchedulerService 接進 App 背景迴圈（已完成）

- `app.go` 加入背景 ticker（startup 啟動、shutdown 停止），定期讀取 settings 並呼叫 `ScanDue` / `RunScheduledScan` 與對應的 update 版本。
- `SaveSettings` 後排程立即依新設定重新計算下一次觸發（驗證 T9.2「domain service 即時套用」、T10.2「更新排程變更後立即重建下次 trigger」）。
- 已完成 startup/shutdown 背景 worker、settings wakeup、service getter 快取；後續可再補更細的 lifecycle 測試。

### 3. T12.2 使用者資料移除選項（已完成）

- Settings 或獨立頁面新增 Remove Settings / Remove Scan Results / Remove Quarantine / Keep User Data。
- 全部是 per-user 檔案操作（`~/Library/Application Support/ClamAVDesktop/...`），不依賴 Phase 1/2。
- 已完成 per-user settings / jobs / results / logs / quarantine 移除；預設不刪 quarantine，刪除 quarantine 需二次確認。

### 4. T11.1 / T11.2 Status Bar / Login item（已完成實作）

- `NSStatusItem` 已透過 darwin cgo / AppKit 建立狀態列選單。
- `Launch at Login` 會優先使用 `SMAppService`，失敗時 fallback 到目前使用者的 `~/Library/LaunchAgents/com.lazyjerry.clamavdesktop.agent.plist`。
- 仍建議後續做實際重新登入驗證。

## 延後／受阻項目

- 實際重新登入驗證 SMAppService / LaunchAgent。
- 實機點擊驗證 NSStatusItem 選單各 action。
- 使用者環境需依 popup 完成 Homebrew ClamAV 安裝、設定與 clamd 啟動。
- Phase 13：Packaging / Signing / Notarization。

## 驗證指令

```bash
cd app
export GOCACHE="${GOCACHE:-$PWD/build/go-cache}"
go vet ./... && go build ./... && go test ./...
# 預期：通過。Runtime resolver 測試已隔離本機 Homebrew ClamAV。

cd app/frontend
npm run build   # 或 npm run typecheck
```
